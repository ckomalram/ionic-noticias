import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.komalramtec.pty',
  appName: 'Noticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
